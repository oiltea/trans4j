name: Publish Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 1.0.0)'
        required: false

  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK + Maven
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven
          server-id: central
          server-username: CENTRAL_USERNAME
          server-password: CENTRAL_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Set version from tag
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          mvn -B versions:set -DnewVersion=$VERSION versions:commit

      - name: Deploy to Maven Central
        run: |
          mvn -B clean deploy -P release
        env:
          CENTRAL_USERNAME: ${{ secrets.CENTRAL_USERNAME }}
          CENTRAL_PASSWORD: ${{ secrets.CENTRAL_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        run: echo "LATEST_VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Update README
        run: |
          sed -i "s/<version>.*<\/version>/<version>${LATEST_VERSION}<\/version>/" README.md
          sed -i "s/<version>.*<\/version>/<version>${LATEST_VERSION}<\/version>/" README_zh.md

      - name: Commit README
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git commit -am "docs: update version to ${LATEST_VERSION}" || echo "No changes"
          git push

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}